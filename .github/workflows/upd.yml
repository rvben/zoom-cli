name: upd dependency health

on:
  schedule:
    - cron: "33 6 * * 2"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    uses: rvben/upd/.github/workflows/dependency-health.yml@a5c4e9e25f7500d9ffd653765fc0cc40f1fc39f3 # v0.8.3
    with:
      upd-version: v0.5.2
      min-age: 7d
      max-bump: minor
      fail-on-blocked: false
